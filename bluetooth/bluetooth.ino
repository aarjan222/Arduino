#include <SoftwareSerial.h>

const int bt_en = 8, bt_vcc = 9, bt_gnd = 10, bt_tx = 11, bt_rx = 12, bt_state = 13;

void setup() {
  pinMode(bt_en, OUTPUT);
  pinMode(bt_vcc, OUTPUT);
  pinMode(bt_gnd, OUTPUT);
  digitalWrite(bt_en, HIGH);
}
void loop() {

digit
}